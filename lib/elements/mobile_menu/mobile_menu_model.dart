import '/elements/tab/tab_widget.dart';
import '/flutter_flow/flutter_flow_util.dart';
import 'mobile_menu_widget.dart' show MobileMenuWidget;
import 'package:flutter/material.dart';

class MobileMenuModel extends FlutterFlowModel<MobileMenuWidget> {
  ///  State fields for stateful widgets in this component.

  // Model for Tab component.
  late TabModel tabModel1;
  // Model for Tab component.
  late TabModel tabModel2;
  // Model for Tab component.
  late TabModel tabModel3;
  // Model for Tab component.
  late TabModel tabModel4;
  // Model for Tab component.
  late TabModel tabModel5;
  // Model for Tab component.
  late TabModel tabModel6;
  // Model for Tab component.
  late TabModel tabModel7;
  // Model for Tab component.
  late TabModel tabModel8;
  // Model for Tab component.
  late TabModel tabModel9;
  // Model for Tab component.
  late TabModel tabModel10;

  @override
  void initState(BuildContext context) {
    tabModel1 = createModel(context, () => TabModel());
    tabModel2 = createModel(context, () => TabModel());
    tabModel3 = createModel(context, () => TabModel());
    tabModel4 = createModel(context, () => TabModel());
    tabModel5 = createModel(context, () => TabModel());
    tabModel6 = createModel(context, () => TabModel());
    tabModel7 = createModel(context, () => TabModel());
    tabModel8 = createModel(context, () => TabModel());
    tabModel9 = createModel(context, () => TabModel());
    tabModel10 = createModel(context, () => TabModel());
  }

  @override
  void dispose() {
    tabModel1.dispose();
    tabModel2.dispose();
    tabModel3.dispose();
    tabModel4.dispose();
    tabModel5.dispose();
    tabModel6.dispose();
    tabModel7.dispose();
    tabModel8.dispose();
    tabModel9.dispose();
    tabModel10.dispose();
  }
}
